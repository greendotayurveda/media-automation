"""
Shared Python package dependencies.
Install with: pip install -r shared/requirements.txt
"""

# Async framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0         # async PostgreSQL driver
psycopg2-binary>=2.9.0  # sync driver for Alembic

# Redis
redis[hiredis]>=5.0.0

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Logging
structlog>=24.0.0

# HTTP client (for metadata/subtitle providers)
httpx>=0.27.0

# Utilities
python-multipart>=0.0.9  # for FastAPI file uploads
python-dotenv>=1.0.0
tenacity>=8.3.0          # retry logic
aiofiles>=23.0.0         # async file I/O
humanize>=4.9.0          # human-readable sizes (e.g. storage reports)
